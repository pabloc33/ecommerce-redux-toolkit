import { sliderData } from "../../src/assets/data/dummyData";

export const sliderInitialState = {
  value: 0,
  length: sliderData.length,
};

export const prevInitialState = {
  value: 1,
  length: sliderData.length,
};
